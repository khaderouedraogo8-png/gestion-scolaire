upstream gestion_backend {
    server backend:5000;
    keepalive 16;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 16M;

    # Frontend SPA (React)
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # API Flask — cookies refresh token (path /api)
    location /api/ {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_pass http://gestion_backend;
    }

    # Cache assets statiques Vite
    location /assets/ {
        root /usr/share/nginx/html;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /sw.js {
        root /usr/share/nginx/html;
        add_header Cache-Control "no-cache";
    }
}

# --- HTTPS (décommenter après Certbot) ---
# server {
#     listen 443 ssl http2;
#     server_name votre-domaine.fr;
#
#     ssl_certificate     /etc/letsencrypt/live/votre-domaine.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/votre-domaine.fr/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#
#     client_max_body_size 16M;
#
#     location / {
#         root /usr/share/nginx/html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#         proxy_pass http://gestion_backend;
#     }
# }
